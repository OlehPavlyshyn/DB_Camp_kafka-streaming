######################### Confluent Properties #########################

# Comma separated address:port values of Kafka brokers endpoints
bootstrap.servers = localhost:9092
# Comma separated http://address:port values of Schema Registry endpoints
schema.registry.url = http://localhost:8081

topic.source=source
topic.sink=sink

